#!/usr/bin/env bash
# Placeholder for macOS .pkg installer (production: sign + notarize InputMixer.driver)
set -euo pipefail
echo "Build the HAL driver from drivers/macos/ and package with pkgbuild."
echo "See drivers/macos/README.md"
